# Core
pandas==2.1.0
pyarrow==14.0.0
python-dotenv==1.0.0

# Kaggle
kaggle==1.6.14

# Azure / Fabric
azure-identity==1.15.0
azure-storage-file-datalake==12.14.0

# dbt retired entirely (ADR-008, supersedes ADR-006 §4) — Gold is warehouse/ T-SQL stored
# procedures, invoked by Data Factory. No dbt-core / dbt-fabric package here (FB5).

# Testing
pytest==7.4.0
pytest-cov==4.1.0

# Utils
PyYAML==6.0.1
