"""Fabric Spark Notebook stub — Silver transform for application_train.

Not yet implemented. Real logic ports from the parent repo's
`glue/glue_silver_application.py` per ADR-002 (PII mask order, unchanged) and
ADR-004 (native Delta MERGE idempotency, new for Fabric). See
`learning/CURRICULUM.md` M2/M3 for the DIY build path.
"""
