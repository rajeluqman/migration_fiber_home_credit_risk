name: 'home_credit_fabric'
version: '1.0.0'
config-version: 2

profile: 'home_credit_fabric'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  home_credit_fabric:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    mart:
      +materialized: table
